services:
  odoo:
    image: odoo:16.0
    depends_on:
      - db
    stdin_open: true
    tty: true
    ports:
      - "127.0.0.1:8069:8069"
      - "127.0.0.1:8072:8072"
    volumes:
      - "odoo-data-16:/var/lib/odoo"

  db:
    image: "postgres:16"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U odoo"]
      interval: 5s
      timeout: 3s
      retries: 5
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - "pg-data-16:/var/lib/postgresql/data/pgdata"

volumes:
  odoo-data-16:
  pg-data-16:
